<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FluentFin.Plugins.Jellyseer.Views.JellyseerRequest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FluentFin.Plugins.Jellyseer.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid CornerRadius="{StaticResource ControlCornerRadius}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.Background>
            <ImageBrush ImageSource="{x:Bind Data.Backdrop, Mode=OneWay}" />
        </Grid.Background>

        <StackPanel
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Padding="8"
            VerticalAlignment="Stretch"
            Spacing="8">
            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{x:Bind Data.Title, Mode=OneWay}" />
            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="{x:Bind Data.RequestedBy, Mode=OneWay}" />
            <StackPanel.Background>
                <SolidColorBrush Opacity="0.5" Color="Black" />
            </StackPanel.Background>
        </StackPanel>

        <Grid
            Grid.Column="1"
            Margin="10"
            CornerRadius="{StaticResource ControlCornerRadius}">
            <Image
                x:Name="PosterImageControl"
                Source="{x:Bind Data.Poster, Mode=OneWay}"
                Stretch="Uniform" />
        </Grid>

    </Grid>
</UserControl>
