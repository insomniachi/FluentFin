<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="FluentFin.Dialogs.Views.IdentifyDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cc="using:CommunityToolkit.WinUI.Converters"
    xmlns:controls="using:FluentFin.Controls"
    xmlns:ctk="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:jm="using:Jellyfin.Sdk.Generated.Models"
    xmlns:local="using:FluentFin.Dialogs.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FluentFin.Dialogs.ViewModels"
    Title="Identify"
    CloseButtonText="Close"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="True"
    IsSecondaryButtonEnabled="True"
    PrimaryButtonCommand="{x:Bind ViewModel.PrimaryButtonExecuteCommand, Mode=OneWay}"
    PrimaryButtonText="{x:Bind ViewModel.PrimaryButtonText, Mode=OneWay}"
    SecondaryButtonCommand="{x:Bind ViewModel.BackCommand, Mode=OneWay}"
    SecondaryButtonText="{x:Bind ViewModel.SecondaryButtonText, Mode=OneWay}"
    Style="{StaticResource DefaultContentDialogStyleWithTitleBackground}"
    mc:Ignorable="d">

    <ScrollViewer MinWidth="500">
        <ctk:SwitchPresenter TargetType="vm:State" Value="{x:Bind ViewModel.ViewState, Mode=OneWay}">
            <ctk:Case Value="Input">
                <StackPanel Spacing="20">
                    <StackPanel.Resources>
                        <Style TargetType="StackPanel">
                            <Setter Property="Spacing" Value="5" />
                        </Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold" />
                        </Style>
                    </StackPanel.Resources>
                    <StackPanel>
                        <TextBlock Text="Name" />
                        <TextBox Text="{x:Bind ViewModel.Name, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Year" />
                        <NumberBox Text="{x:Bind ViewModel.Year, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="AniList Series Id" />
                        <TextBox Text="{x:Bind ViewModel.AnilistSeriesId, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="IMDb Id" />
                        <TextBox Text="{x:Bind ViewModel.ImbdId, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="TheMovieDb Movie Id" />
                        <TextBox Text="{x:Bind ViewModel.TmdbMovieId, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="TheMovieDb Box Set Id" />
                        <TextBox Text="{x:Bind ViewModel.TmdbBoxSetId, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="TheTVDB Box Set Id" />
                        <TextBox Text="{x:Bind ViewModel.TvdbBoxSetId, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="TheTVDB Numerical Movie Id" />
                        <TextBox Text="{x:Bind ViewModel.TvdbNumericalMovieId, Mode=TwoWay}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="TheTVDB Slug Movie Id" />
                        <TextBox Text="{x:Bind ViewModel.TvdbSlugMovieId, Mode=TwoWay}" />
                    </StackPanel>
                </StackPanel>
            </ctk:Case>
            <ctk:Case Value="Loading">
                <Grid>
                    <ProgressRing
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        IsIndeterminate="True" />
                </Grid>
            </ctk:Case>
            <ctk:Case Value="Result">
                <ItemsView
                    ItemsSource="{x:Bind ViewModel.Results, Mode=OneWay}"
                    SelectionChanged="ItemsView_SelectionChanged"
                    SelectionMode="Single">
                    <ItemsView.ItemTemplate>
                        <DataTemplate x:DataType="jm:RemoteSearchResult">
                            <ItemContainer>
                                <Grid ColumnSpacing="15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <controls:LoadingImage
                                        CornerRadius="{StaticResource ControlCornerRadius}"
                                        Source="{x:Bind ImageUrl}"
                                        Stretch="UniformToFill" />
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="{x:Bind Name}" />
                                        <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Text="{x:Bind ProductionYear}" />
                                        <TextBlock
                                            Style="{ThemeResource CaptionTextBlockStyle}"
                                            Text="{x:Bind SearchProviderName}"
                                            TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Grid>
                            </ItemContainer>
                        </DataTemplate>
                    </ItemsView.ItemTemplate>
                    <ItemsView.Layout>
                        <StackLayout Orientation="Vertical" Spacing="5" />
                    </ItemsView.Layout>
                </ItemsView>
            </ctk:Case>
        </ctk:SwitchPresenter>
    </ScrollViewer>
</ContentDialog>
