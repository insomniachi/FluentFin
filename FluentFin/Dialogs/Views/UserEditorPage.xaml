<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="FluentFin.Dialogs.Views.UserEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FluentFin.Dialogs.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FluentFin.Core.ViewModels"
    mc:Ignorable="d">

    <Grid Margin="36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="20">
            <TextBlock Style="{ThemeResource SubheaderTextBlockStyle}" Text="{x:Bind ViewModel.User.Name, Mode=OneWay}" />
            <SelectorBar x:Name="SelectorBar" SelectionChanged="SelectorBar_SelectionChanged">
                <SelectorBarItem Tag="{x:Bind vm:UserEditorSection.Profile}" Text="Profile" />
                <SelectorBarItem Tag="{x:Bind vm:UserEditorSection.Access}" Text="Access" />
                <SelectorBarItem Tag="{x:Bind vm:UserEditorSection.ParentalControl}" Text="Parental Control" />
                <SelectorBarItem Tag="{x:Bind vm:UserEditorSection.Password}" Text="Password" />
            </SelectorBar>
        </StackPanel>

        <Frame x:Name="NavFrame" Grid.Row="1" />
    </Grid>

</Page>
