<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="FluentFin.Views.UserEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FluentFin.Dialogs.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FluentFin.Core.ViewModels"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer>
            <Grid MaxWidth="1000" Margin="36,36,36,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Margin="{StaticResource SmallTopBottomMargin}" Spacing="20">
                    <TextBlock Style="{ThemeResource SubheaderTextBlockStyle}" Text="{x:Bind ViewModel.User.Name, Mode=OneWay}" />
                    <SelectorBar x:Name="SelectorBar" SelectionChanged="SelectorBar_SelectionChanged">
                        <SelectorBarItem IsSelected="{x:Bind IsSelected(ViewModel.SelectedSectionViewModel, vm:UserEditorSection.Profile), Mode=OneWay}" Text="Profile" />
                        <SelectorBarItem IsSelected="{x:Bind IsSelected(ViewModel.SelectedSectionViewModel, vm:UserEditorSection.Access), Mode=OneWay}" Text="Access" />
                        <SelectorBarItem IsSelected="{x:Bind IsSelected(ViewModel.SelectedSectionViewModel, vm:UserEditorSection.ParentalControl), Mode=OneWay}" Text="Parental Control" />
                        <SelectorBarItem IsSelected="{x:Bind IsSelected(ViewModel.SelectedSectionViewModel, vm:UserEditorSection.Password), Mode=OneWay}" Text="Password" />
                    </SelectorBar>
                </StackPanel>

                <Frame x:Name="NavFrame" Grid.Row="1" />
            </Grid>
        </ScrollViewer>

        <StackPanel
            Grid.Row="1"
            MaxWidth="1000"
            Padding="16"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            FlowDirection="RightToLeft"
            Orientation="Horizontal"
            Spacing="16">
            <Button Command="{x:Bind ViewModel.SaveCommand}" Content="Save" />
            <Button Command="{x:Bind ViewModel.ResetCommand}" Content="Reset" />
        </StackPanel>
    </Grid>

</Page>
